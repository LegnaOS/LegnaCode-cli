// Stub: Undercover auto callout — gated by "external" === 'ant' (Anthropic internal only)
import React from 'react'
export function UndercoverAutoCallout(): React.ReactElement | null {
  return null
}
