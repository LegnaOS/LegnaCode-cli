// Stub: Ant model switch callout — gated by "external" === 'ant' (Anthropic internal only)
import React from 'react'
export function AntModelSwitchCallout(): React.ReactElement | null {
  return null
}
export function shouldShowModelSwitchCallout(): boolean {
  return false
}
