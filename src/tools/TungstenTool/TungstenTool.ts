// Stub: TungstenTool — Anthropic internal tool, not included in leaked source
export const TUNGSTEN_TOOL_NAME = 'TungstenTool'
export const TungstenTool = {
  name: TUNGSTEN_TOOL_NAME,
  description: 'Tungsten tool (internal)',
  inputSchema: { type: 'object' as const },
}
