// Stub: modifiers-napi — native keyboard modifier key detection.
// Used for detecting Shift/Ctrl/Alt held during keypress events.

export function prewarm(): void {}

export function isModifierPressed(_modifier: string): boolean {
  return false
}
