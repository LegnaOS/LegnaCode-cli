// Stub: @anthropic-ai/claude-agent-sdk — Anthropic's Agent SDK
// Only a type import is used in the codebase (PermissionMode).

export type PermissionMode = 'default' | 'plan' | 'bypassPermissions' | 'auto'
